services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    image: miner-web
    environment:
      ALLOWED_ORIGINS: http://localhost:5174,http://127.0.0.1:5174
    ports:
      - "8000:7860"
